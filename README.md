# Qt Builder

Downloads and builds Qt 6 from source.

## Notes

CMake, Ninja, Python, and Perl must be in the users path.

### Linux

Run `configure.sh` then `build.sh`.

### Windows

Modify the VS install path based on version installed (Community/Professional).
Run `configure.sh` from Git Bash then `build.bat` from Powershell.

